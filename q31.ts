let users: string[] = ['john', 'alice', 'bob', 'jane'];

if (users.length > 0) {
   
    users.forEach(user => {
        console.log(`Hello, ${user}!`);
    });
} else {
    
    console.log("We need to find some users!");
}

if(users.length>0)
{
    users.forEach(user=>{

    });
}

users = [];


if (users.length > 0) {
    
    users.forEach(user => {
        
        console.log(`Hello, ${user}!`);
    });
} else {
   
    console.log("We need to find some users!");
}
